import { useState } from "react"; export default function Quiz({ questions, onComplete }) { const [step, setStep] = useState(0); const [answers, setAnswers] = useState([]); function handleAnswer(option) { const newAnswers = [...answers, option]; setAnswers(newAnswers); if (step + 1 === questions.length) { onComplete(newAnswers); } else { setStep(step + 1); } } return ( <div className="card"> <h2>{questions[step].question}</h2> {questions[step].options.map((opt, i) => ( <button key={i} onClick={() => handleAnswer(opt)} className="quiz-button" > {opt} </button> ))} </div> ); }